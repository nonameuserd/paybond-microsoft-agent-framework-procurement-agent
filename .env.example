# Copy to .env.local after `paybond login`
PAYBOND_API_KEY=
PAYBOND_GATEWAY_URL=https://api.paybond.ai

# Only required for the live LLM kickoff in agent.py (Azure AI Foundry).
# AZURE_AI_PROJECT_ENDPOINT=
# AZURE_AI_MODEL_DEPLOYMENT_NAME=
